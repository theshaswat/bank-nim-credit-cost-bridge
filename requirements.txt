pandas>=2.0
matplotlib>=3.8
reportlab>=4.0
# src/build_pdf.py reads each chart's real pixel dimensions through Pillow so
# the memo scales it by its own aspect ratio. reportlab pulls Pillow in anyway,
# but the import is direct, so the dependency is declared directly.
pillow>=10.0

# The shared reconciliation library this project is built on. Installing it
# from git is what makes `pip install -r requirements.txt` sufficient on a
# fresh clone -- the scripts do a plain `import indfin` and do not manipulate
# sys.path, so the package has to genuinely be installed.
indfin @ git+https://github.com/theshaswat/indfin.git@main
